"""Framework integrations for memorywire.

- :mod:`memorywire.integrations.langchain` — a drop-in LangChain chat-message history that
  persists to memorywire with **automatic provenance** (message role -> ``source``), so
  ``recover`` works without any manual tagging.
"""
